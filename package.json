{
    "name": "unsafe_test",
    "description": "Runs arbitrary code, which isn't ordinarily possible within a Chrome extension",
    "version": "1.0.0",
    "type": "commonjs",
    "license": "MIT",
    "private": true,
    "repository": {
        "url": "git+https://github.com/Anonymous-Humanoid/chromium-extension-eval-sample.git",
        "type": "git"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.320",
        "devtools-protocol": "^0.0.1532728"
    }
}
